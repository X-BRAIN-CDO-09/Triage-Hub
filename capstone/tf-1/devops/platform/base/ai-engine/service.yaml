apiVersion: v1
kind: Service
metadata:
  name: tf1-api
  namespace: triage-hub
  labels:
    app: tf1-api
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: tf1-api
  type: ClusterIP
