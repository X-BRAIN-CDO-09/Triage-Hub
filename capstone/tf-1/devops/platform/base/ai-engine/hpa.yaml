apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tf1-api-hpa
  namespace: triage-hub
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: tf1-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
