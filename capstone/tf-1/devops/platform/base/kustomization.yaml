apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - ai-engine/serviceaccount.yaml
  - ai-engine/service.yaml
  - ai-engine/rollout.yaml
  - ai-engine/analysis-template.yaml
  - ai-engine/targetgroupbinding.yaml
  - ai-engine/hpa.yaml
  - ai-engine/pdb.yaml
  - ai-engine/servicemonitor.yaml
  # Service/Endpoints trỏ ra EC2 monitoring (IP do CI tự patch từ SSM
  # /triage-hub/sandbox/prometheus_ip mỗi khi IP đổi — xem ci-infra.yml).
  - ai-engine/prometheus-external-service.yaml
  - ai-engine/loki-external-service.yaml
  - ai-worker/serviceaccount.yaml
  - ai-worker/deployment.yaml
  - ai-worker/scaledobject.yaml
  - networking/netpol-api.yaml
  - networking/netpol-worker.yaml
  - networking/netpol-default.yaml
  - secrets/cluster-secret-store.yaml
  - secrets/external-secret.yaml
  - security/cluster-image-policy.yaml
  - security/rbac.yaml
  # Gatekeeper: ConstraintTemplate (sync-wave -1) phải apply trước Constraint
  # (sync-wave 1). Tất cả enforcementAction=dryrun ban đầu — xem comment trong
  # constraints.yaml về lộ trình chuyển sang deny.
  - security/gatekeeper/constraint-templates.yaml
  - security/gatekeeper/config-sync.yaml
  - security/gatekeeper/constraints.yaml
